{
  "name": "md-syntax",
  "version": "1.0.0",
  "description": "markdown 语法说明",
  "main": "index.js",
  "scripts": {
    "build": "node build.js",
    "deploy": "yarn build && copy CNAME dist && gh-pages -d dist && rimraf dist"
  },
  "keywords": [
    "rxliuli",
    "markdown",
    "md"
  ],
  "author": "rxliuli",
  "license": "ISC",
  "dependencies": {
    "marked": "^0.5.2"
  },
  "devDependencies": {
    "copy": "^0.3.2",
    "gh-pages": "^2.0.1",
    "rimraf": "^2.6.3"
  }
}
